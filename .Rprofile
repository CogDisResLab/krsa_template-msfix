r_profile <- file.path(Sys.getenv("HOME"), ".Rprofile")

if (file.exists(r_profile)) {
  source(r_profile) # nolint: undesirable_function_linter.
}

source(file.path("renv", "activate.R")) # nolint: undesirable_function_linter.
